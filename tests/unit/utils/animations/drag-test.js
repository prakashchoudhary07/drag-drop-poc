import animationsDrag from 'drag-drop/utils/animations/drag';
import { module, test } from 'qunit';

module('Unit | Utility | animations/drag', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = animationsDrag();
    assert.ok(result);
  });
});
